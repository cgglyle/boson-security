server:
  port: 9001

spring:
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        show_sql: true
    open-in-view: false
  mvc:
    problem-details:
      enabled: true
